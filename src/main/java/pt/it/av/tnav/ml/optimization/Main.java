package pt.it.av.tnav.ml.optimization;

public class Main {
	public static void main(String[] args) {
		/*List<Chromosome> population = new ArrayList<Chromosome>();
		long elapsed = 0;
		for (int i = 0; i < 50; i++)
			population.add(new QueenChromosome(4));
		long startTime = System.currentTimeMillis();
		population = GPGA.optimize(population,
				new TournamentSelection(),
				new FitnessTermination(1.0));
		elapsed = elapsed + (System.currentTimeMillis() - startTime);
		Chromosome solution = Collections.min(population);
		System.out.println(solution);
		population.clear();*/
	}
}